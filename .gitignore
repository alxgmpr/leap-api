node_modules/
dist/
# Anchored: the generated site is /site only. `site/` unanchored also matched
# lib/site (the generator's source), which is tracked.
/site/
raw/
*.local.json
.DS_Store
CLAUDE.md

# SDD process workspace (also covered by global gitignore)
.superpowers/

# AI process artifacts stay local (plans, specs, agent workspaces)
docs/plans/
docs/superpowers/
