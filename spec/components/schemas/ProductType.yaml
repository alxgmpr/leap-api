type: string
description: |
  Project's product line. The firmware extraction did not recover the full
  enum; members below were recovered from Project.ProductType observed on
  RA3 v03.247 and Caseta v01.123 (via /project). Unlisted values are
  possible.
enum:
  - Lutron HWQS Project
  - Lutron Smart Bridge Project
