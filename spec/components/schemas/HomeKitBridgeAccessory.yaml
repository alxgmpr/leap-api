# Straight copy from _generated/HomeKitBridgeAccessory.yaml -- dependency
# of HomeKitProperties.yaml. Matches the one captured instance (RA3
# v03.247, via /service: HomeKitProperties.BridgeAccessory.SerialNumber).
type: object
properties:
  SerialNumber:
    type: string
required:
  - SerialNumber
