type: string
description: |
  The firmware extraction did not recover the full enum; `Admin` is the only
  value observed, on a single processor's own session during Task 8's sweep
  campaign (`fixtures/sweep-read.json`, `/clientsetting`). Unlisted values
  are possible -- a non-admin integrator/guest role, for instance, is
  plausible but unconfirmed by any capture in this project's corpus.
enum:
  - Admin
