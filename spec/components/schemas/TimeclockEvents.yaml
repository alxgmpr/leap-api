# Hand-authored: GET /timeclockevent's collection response. The firmware
# route-table extraction only recovered /timeclockevent/{timeclockeventId}
# and its query/paging variants; the bare collection route
# `/timeclockevent` is entirely absent from _generated/timeclockevent.yaml
# -- not present in any form, mangled or otherwise, and confirmed real by
# captured traffic, which shows the wire body is `{"TimeclockEvents":
# [...]}`. 200 OK on RA3 v03.247; 204 NoContent on Caseta v01.123.
type: array
items:
  $ref: "#/components/schemas/TimeclockEvent"
