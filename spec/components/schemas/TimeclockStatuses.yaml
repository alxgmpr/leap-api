# Hand-authored: GET /timeclock/status's collection response. The firmware
# route-table extraction recorded a mangled `/timeclockstatus` family
# instead (see _generated/timeclockstatus.yaml, left in staging per this
# task's instructions -- its own bare `/timeclockstatus` entry only carries
# subscription markers, no recovered response type). Captured traffic
# shows the real wire URL is `/timeclock/status`, body key
# `TimeclockStatuses`. 200 OK on RA3 v03.247; 204 NoContent on Caseta
# v01.123.
type: array
items:
  $ref: "#/components/schemas/TimeclockStatus"
