type: string
description: |
  Category of a virtual/scene button. The firmware extraction did not
  recover the full enum; members below were recovered from the 6
  `/vbutton/{id}`-shaped objects embedded in Caseta v01.123's
  `/virtualbutton` collection response (see VirtualButton.yaml). Never
  observed on RA3 v03.247 (204 NoContent on this route). Unlisted values
  are possible.
enum:
  - Bedside
  - Kitchen
