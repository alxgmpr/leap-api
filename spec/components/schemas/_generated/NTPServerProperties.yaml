type: object
properties:
  PropertiesType:
    type: string
    description: "TODO(enum): members of NTPServerPropertiesType were not recovered
      from the firmware extraction"
  Endpoints:
    type: array
    items:
      $ref: "#/components/schemas/NTPServerEndpoint"
required:
  - PropertiesType
