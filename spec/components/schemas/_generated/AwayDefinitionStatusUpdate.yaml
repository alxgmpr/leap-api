type: object
properties:
  EnabledState:
    type: string
    description: "TODO(enum): members of SystemAwayEnabledState were not recovered
      from the firmware extraction"
required:
  - EnabledState
