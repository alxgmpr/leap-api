type: object
properties:
  href:
    type: string
  RuleName:
    type: string
  EnabledState:
    type: string
    description: "TODO(enum): members of EnabledState were not recovered from the
      firmware extraction"
required:
  - RuleName
  - EnabledState
