type: object
properties:
  ParentAddressedState:
    type: string
    description: "TODO(enum): members of AddressedState were not recovered from the
      firmware extraction"
required:
  - ParentAddressedState
