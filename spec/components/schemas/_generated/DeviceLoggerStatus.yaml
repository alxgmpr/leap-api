type: object
properties:
  href:
    type: string
  LoggingLevel:
    type: string
    description: "TODO(enum): members of LoggingLevel were not recovered from the
      firmware extraction"
  Expiration:
    type: string
    pattern: ^P(?:\d+D)?(?:T(?:\d+H)?(?:\d+M)?(?:\d+(?:\.\d+)?S)?)?$
    example: PT2S
  DataShippingRules:
    type: array
    items:
      $ref: "#/components/schemas/DataShippingRule"
required:
  - LoggingLevel
