type: object
properties:
  ScheduleType:
    type: string
    description: "TODO(enum): members of HvacScheduleType were not recovered from
      the firmware extraction"
  WeeklySchedule:
    $ref: "#/components/schemas/WeeklySchedule"
required:
  - ScheduleType
