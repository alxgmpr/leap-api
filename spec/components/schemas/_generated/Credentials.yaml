type: object
properties:
  href:
    type: string
  CredentialType:
    type: string
    description: "TODO(enum): members of CredentialType were not recovered from the
      firmware extraction"
required:
  - CredentialType
