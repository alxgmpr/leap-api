type: object
properties:
  SwitchedLevel:
    type: string
    description: "TODO(enum): members of SwitchedLevel were not recovered from the
      firmware extraction"
  DelayTime:
    type: string
    pattern: ^P(?:\d+D)?(?:T(?:\d+H)?(?:\d+M)?(?:\d+(?:\.\d+)?S)?)?$
    example: PT2S
required:
  - SwitchedLevel
