type: object
properties:
  Frequency:
    type: string
    description: |
      TODO(enum): members of Frequency were not recovered from the firmware
      extraction, and hardware has produced exactly one: `"434"`, twice --
      once in each Caseta corpus, both via
      /link/1/associatedlinknode/expanded. No RA3 corpus carries a
      ChannelSet. This is a radio band in MHz, and Lutron's Clear Connect
      radios are region-specific -- 434 is what these North American
      bridges report, not the vocabulary of the type. Closing on it would
      reject hardware sold elsewhere.
    x-observed-values:
      - "434"
required:
  - Frequency
