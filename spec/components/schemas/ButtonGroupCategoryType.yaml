type: string
description: |
  Button group category. The firmware extraction did not recover the full
  enum; members below were recovered from ButtonGroupCategory.Type observed
  on RA3 v03.247 (4 instances) and Caseta v01.123 (10 instances), via
  /buttongroup and /device/{deviceId}/buttongroup/expanded. Unlisted values
  are possible.
enum:
  - Lights
