# Straight copy from _generated/GoogleHomeProperties.yaml -- matches
# captured traffic exactly. Observed on both platforms as the flat
# `GoogleHomeProperties` key on a `/service` array entry with
# `Type: "GoogleHome"` (RA3 v03.247: both `Configuration` and
# `DataSummary`; Caseta v01.123: `DataSummary` only, consistent with both
# fields already being optional here). See Service.yaml.
type: object
properties:
  Configuration:
    $ref: "#/components/schemas/HyperReference"
  DataSummary:
    $ref: "#/components/schemas/HyperReference"
