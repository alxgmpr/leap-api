type: object
properties:
  CCOLevel:
    type: string
    description: |
      TODO(enum): members of CCOLevel were not recovered from the firmware
      extraction. This is command-parameter input and no corpus observes it
      directly; the only value ever seen for this firmware type
      (`leapobj.CCOLevel`) is `Open`, on ZoneStatus.CCOLevel. One value is
      not a set -- see ZoneStatus.yaml.
  DelayTime:
    type: string
    pattern: ^P(?=.*\d)(?:\d+D)?(?:T(?:\d+H)?(?:\d+M)?(?:\d+(?:\.\d+)?S)?)?$
    example: PT2S
required:
  - CCOLevel
