type: string
description: |
  Contact's role on the project. The firmware extraction did not recover
  the full enum; members below were recovered from Contact.Role observed on
  RA3 v03.247 (via /project/contactinfo; Caseta v01.123 returns 204
  NoContent on this route, so unconfirmed there). Unlisted values are
  possible.
enum:
  - Installer
