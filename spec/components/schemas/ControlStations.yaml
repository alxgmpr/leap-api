# Hand-authored: GET /area/{areaId}/associatedcontrolstation's collection
# response. No responseType recovered from the firmware extraction for this
# route. Captured traffic resolves both the shape and the body key: RA3
# v03.247 returns 200 OK with body key "ControlStations" for 9 of 11
# non-root areas (the other 2 -- the root grouping area /area/3, and
# /area/1256, which has no control stations -- return 204 NoContent).
# Caseta v01.123 returns 204 NoContent on every area, consistent with its
# flat-list navigation model rather than RA3's area-walk (see
# docs/platforms.md). ControlStation.yaml already ships and matches the
# array items observed here.
type: array
items:
  $ref: "#/components/schemas/ControlStation"
