# Hand-authored: GET /area/{areaId}/associatedcontrolstation's collection
# response. No responseType recovered from the firmware extraction for this
# route. Captured traffic resolves both the shape and the body key: RA3
# v03.247 probes 12 areas total (1 root grouping area, /area/3, plus 11
# non-root); of the 11 non-root areas, 10 return 200 OK with body key
# "ControlStations" and 1 (/area/1256, which has no control stations)
# returns 204 NoContent. The root area, /area/3, also returns 204
# NoContent, so 10 of the full 12 areas return 200 overall. Caseta v01.123
# returns 204 NoContent on every area, consistent with its flat-list
# navigation model rather than RA3's area-walk (see docs/platforms.md).
# ControlStation.yaml already ships and matches the array items observed
# here.
type: array
items:
  $ref: "#/components/schemas/ControlStation"
