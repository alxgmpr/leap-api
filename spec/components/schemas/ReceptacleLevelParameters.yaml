type: object
properties:
  ReceptacleLevel:
    type: string
    description: |
      TODO(enum): members of ReceptacleLevel were not recovered from the firmware
      extraction, and nothing anywhere in the corpus carries this firmware type
      (`leapobj.ReceptacleLevel`): neither platform has a receptacle zone,
      so ZoneStatus.ReceptacleLevel is unobserved too.
  DelayTime:
    type: string
    pattern: ^P(?=.*\d)(?:\d+D)?(?:T(?:\d+H)?(?:\d+M)?(?:\d+(?:\.\d+)?S)?)?$
    example: PT2S
required:
  - ReceptacleLevel
