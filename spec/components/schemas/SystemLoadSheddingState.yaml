type: string
description: |
  Load shedding activation state. The firmware extraction did not recover
  the full enum; members below were recovered from
  SystemLoadSheddingStatus.State observed on RA3 v03.247 (via
  /system/loadshedding/status; unconfirmed on Caseta, which returns 405 on
  this route). Unlisted values are possible.
enum:
  - Disabled
