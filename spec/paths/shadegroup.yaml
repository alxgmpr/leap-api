# HAND-AUTHORED (probe-derived). The firmware route table has no
# `/shadegroup/...` route under that spelling; it records only the
# concatenation `/shadegroupstatus` (GET, responseType ShadeGroupStatus),
# one of the 51 concatenation-suspect spellings. The in-doubt sweep
# (2026-08-31, read-only, fixtures/spec-read-indoubt.json, RA3 v03.249)
# settled it: the concatenated form answers `400 "This request is not
# supported"`, `/shadegroup/status` answers `405 MethodNotAllowed` — the
# route exists but refuses a ReadRequest on this system. No body has ever
# been observed, so no response schema is asserted.
/shadegroup/status:
  get:
    operationId: getShadeGroupStatus
    tags:
      - shadegroup
    x-leap-communique-type: ReadRequest
    responses:
      "200":
        description: Success
    summary: List shade group statuses
